#该文件是来展示makefile的变量变化
#这里可以很好的展现出，:=是将变量立即确定的，而=是使用到才确定的。
A := $(C)
B  = $(C)
C  = 123
D  = 100xsn
D ?=100bier #?=代表着上面定义了，这里就不会定义了。

bll:
	@echo A=$(A)
	@echo B=$(B)
	@echo C=$(C)
	@echo D=$(D)
C +=abc               #这里+=代表着附加，将上面的字符串连接在一块
